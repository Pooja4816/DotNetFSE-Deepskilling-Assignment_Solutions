# ReactJS-HOL 4: blogapp

## Overview
Class-based React component `Posts` fetching post data from `jsonplaceholder.typicode.com/posts` using `componentDidMount()` and `loadPosts()`, handling errors with `componentDidCatch()`.

## Running the Application
```bash
npm install
npm start
```
