# ReactJS-HOL 5: cohortdashboard

## Overview
React Cohort Dashboard styled using CSS Modules (`CohortDetails.module.css`) with dynamic font colors (`green` for ongoing, `blue` for other statuses) applied to `<h3>` elements based on status.

## Running the Application
```bash
npm install
npm start
```
