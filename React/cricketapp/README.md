# ReactJS-HOL 9: cricketapp

## Overview
React application demonstrating ES6 features (`.map()`, arrow functions, destructuring, array spread operator) across `ListofPlayers` and `IndianPlayers` components toggled by a `flag` variable.

## Running the Application
```bash
npm install
npm start
```
