# ReactJS-HOL 2: StudentApp

## Overview
Student Management Portal React application demonstrating class components (`Home`, `About`, `Contact`) invoked from `App.js`.

## Running the Application
```bash
npm install
npm start
```
