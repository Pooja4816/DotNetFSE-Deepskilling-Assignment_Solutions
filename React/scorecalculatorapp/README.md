# ReactJS-HOL 3: scorecalculatorapp

## Overview
Functional React component `CalculateScore` calculating the average score percentage based on `Total` and `Goal` props, styled using `mystyle.css`.

## Running the Application
```bash
npm install
npm start
```
