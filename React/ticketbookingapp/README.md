# ReactJS-HOL 12: ticketbookingapp

## Overview
Flight ticket booking application (`ticketbookingapp`) demonstrating conditional rendering using element variables. Toggles between `GuestPage` (browse mode) and `UserPage` (booking mode) based on login/logout state.

## Running the Application
```bash
npm install
npm start
```
