# ReactJS-HOL 10: officespacerentalapp

## Overview
React JSX application `officespacerentalapp` rendering office space details, image attributes, array maps, and dynamic inline CSS for rental costs (`Red` for < ₹60,000 and `Green` for >= ₹60,000).

## Running the Application
```bash
npm install
npm start
```
